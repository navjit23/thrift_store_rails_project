<% if session[:cart].present? %>
  <h2>Cart</h2>
  <ul>
    <% session[:cart].each do |product_id, quantity| %>
      <li>
        <%= Product.find(product_id).name %> - Quantity: <%= quantity %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Your cart is empty.</p>
<% end %>

